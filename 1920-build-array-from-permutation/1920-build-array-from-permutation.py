class Solution(object):
    def buildArray(self, nums):
        a=[]
        for i in nums:
            print(nums[i])
            a.append(nums[i])
            
        return a
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        